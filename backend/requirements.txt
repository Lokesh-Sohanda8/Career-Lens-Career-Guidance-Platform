# CareerLens Backend — Reference Requirements
# This file is intentionally kept as a reference dependency list.
# pyproject.toml remains the primary dependency/configuration source.

# -----------------------------
# Web / API
# -----------------------------
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
python-multipart>=0.0.20,<1.0

# -----------------------------
# Configuration / Validation
# -----------------------------
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0
email-validator>=2.2,<3.0

# -----------------------------
# Database / ORM / Migrations
# -----------------------------
SQLAlchemy>=2.0,<3.0
asyncpg>=0.30,<1.0
alembic>=1.14,<2.0

# -----------------------------
# Authentication / Security
# -----------------------------
python-jose[cryptography]>=3.3,<4.0
passlib[bcrypt]>=1.7,<2.0
bcrypt>=4.2,<5.0

# -----------------------------
# HTTP / AI Provider
# -----------------------------
httpx>=0.28,<1.0

# -----------------------------
# Testing
# -----------------------------
pytest>=8.3,<9.0
pytest-asyncio>=0.25,<1.0
httpx>=0.28,<1.0

# -----------------------------
# Development / Quality
# -----------------------------
ruff>=0.9,<1.0
black>=25.0,<26.0
mypy>=1.15,<2.0
